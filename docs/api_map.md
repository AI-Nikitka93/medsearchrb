# API Map

## Status
- No public XHR endpoint is used in the initial slice.
- Current scrapers work from SSR HTML pages.

## Future candidates
- `YDoc`: `data-schedule-storage` suggests internal schedule data exists in page payload, but we do not call any private endpoint in MVP.
- `MedArt`: current implementation relies on public HTML and JSON-LD only.
